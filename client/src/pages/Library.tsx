import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { BookOpen, ChevronRight, Clock3, Search, Sparkles } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";

const categories = ["All", "Foundations", "Meetings", "Presence", "Influence", "Leadership", "Career"];

export default function Library() {
  const [, navigate] = useLocation();
  const [category, setCategory] = useState("All");
  const [query, setQuery] = useState("");
  const library = trpc.program.library.useQuery({ category, query });
  const lessons = library.data?.lessons ?? [];
  return <div className="workspace-content text-foreground"><section className="library-heading"><p className="eyebrow">The program library</p><h1 className="mt-3 font-display text-4xl tracking-[-.03em] text-white sm:text-5xl">Every skill, made practiceable.</h1><p className="mt-4 max-w-2xl leading-7 text-muted-foreground">Twenty-eight concise lessons for the conversations that shape your work, plus deeper modules for the moments that demand more.</p></section><div className="mt-8 flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div className="relative max-w-md flex-1"><Search className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search lessons and skills" className="h-12 border-border bg-card pl-11 text-foreground placeholder:text-muted-foreground"/></div><div className="hide-scrollbar flex gap-2 overflow-x-auto pb-1">{categories.map(item => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm transition ${category === item ? "bg-primary font-semibold text-primary-foreground" : "bg-card text-muted-foreground hover:bg-accent hover:text-accent-foreground"}`}>{item}</button>)}</div></div><section className="mt-7 grid gap-3 md:grid-cols-2 2xl:grid-cols-3">{library.isLoading ? Array.from({ length: 6 }).map((_, index) => <div key={index} className="lesson-card animate-pulse bg-card"/>) : lessons.map(lesson => <article key={lesson.day} className="lesson-card group"><div className="flex items-start justify-between"><span className="lesson-day">Day {String(lesson.day).padStart(2, "0")}</span><span className="rounded-full bg-card px-2.5 py-1 text-xs text-muted-foreground">{lesson.category}</span></div><h2 className="mt-8 text-xl font-semibold tracking-tight text-white">{lesson.title}</h2><p className="mt-3 min-h-12 text-sm leading-6 text-muted-foreground">{lesson.objective}</p><div className="mt-6 flex items-center justify-between border-t border-border pt-4"><span className="inline-flex items-center gap-2 text-xs text-muted-foreground"><Clock3 className="h-3.5 w-3.5"/>{lesson.duration}</span><button onClick={() => navigate(`/lessons/${lesson.day}`)} className="inline-flex items-center text-sm font-medium text-primary transition group-hover:translate-x-1">Open <ChevronRight className="ml-1 h-4 w-4"/></button></div></article>)}</section><section className="mt-9 rounded-2xl border border-chart-2/35 bg-chart-2/10 p-6 sm:p-8"><div className="grid items-center gap-6 md:grid-cols-[auto_1fr_auto]"><span className="grid h-12 w-12 place-items-center rounded-xl bg-chart-2 text-primary-foreground"><Sparkles className="h-5 w-5"/></span><div><p className="text-sm font-semibold text-chart-2">Beyond the daily program</p><h2 className="mt-1 text-xl font-semibold text-white">Bonus modules for the conversations that carry more weight.</h2></div><Button onClick={() => setCategory("Leadership")} variant="outline" className="border-chart-2/50 bg-transparent text-chart-2 hover:bg-chart-2/15 hover:text-white">Explore modules <BookOpen className="ml-2 h-4 w-4"/></Button></div></section></div>;
}
