import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { BookOpen, ChevronRight, Clock3, Search, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";

const categories = ["All", "Foundations", "Meetings", "Presence", "Influence", "Leadership", "Career"];

export default function Library() {
  const [, navigate] = useLocation();
  const [category, setCategory] = useState("All");
  const [query, setQuery] = useState("");
  const library = trpc.program.library.useQuery({ category, query });
  const lessons = library.data?.lessons ?? [];
  return <div className="workspace-content"><section className="library-heading"><p className="eyebrow">The program library</p><h1 className="mt-3 font-display text-4xl tracking-[-.03em] text-white sm:text-5xl">Every skill, made practiceable.</h1><p className="mt-4 max-w-2xl leading-7 text-[#a5beb6]">Twenty-eight concise lessons for the conversations that shape your work, plus deeper modules for the moments that demand more.</p></section><div className="mt-8 flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div className="relative max-w-md flex-1"><Search className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-[#9bb4a9]"/><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search lessons and skills" className="h-12 border-white/12 bg-white/[.04] pl-11 text-white placeholder:text-[#78948a]"/></div><div className="hide-scrollbar flex gap-2 overflow-x-auto pb-1">{categories.map(item => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm transition ${category === item ? "bg-[#a5f4c9] font-semibold text-[#09231b]" : "bg-white/[.05] text-[#b2c6be] hover:bg-white/10 hover:text-white"}`}>{item}</button>)}</div></div><section className="mt-7 grid gap-3 md:grid-cols-2 2xl:grid-cols-3">{library.isLoading ? Array.from({ length: 6 }).map((_, index) => <div key={index} className="lesson-card animate-pulse bg-white/[.04]" />) : lessons.map(lesson => <article key={lesson.day} className="lesson-card group"><div className="flex items-start justify-between"><span className="lesson-day">Day {String(lesson.day).padStart(2, "0")}</span><span className="rounded-full bg-white/[.06] px-2.5 py-1 text-xs text-[#a5beb6]">{lesson.category}</span></div><h2 className="mt-8 text-xl font-semibold tracking-tight text-white">{lesson.title}</h2><p className="mt-3 min-h-12 text-sm leading-6 text-[#a5beb6]">{lesson.objective}</p><div className="mt-6 flex items-center justify-between border-t border-white/9 pt-4"><span className="inline-flex items-center gap-2 text-xs text-[#9bb4a9]"><Clock3 className="h-3.5 w-3.5"/>{lesson.duration}</span><button onClick={() => navigate(`/lessons/${lesson.day}`)} className="inline-flex items-center text-sm font-medium text-[#a5f4c9] transition group-hover:translate-x-1">Open <ChevronRight className="ml-1 h-4 w-4" /></button></div></article>)}</section><section className="mt-9 rounded-2xl border border-[#f4bc79]/20 bg-[#f4bc79]/[.07] p-6 sm:p-8"><div className="grid items-center gap-6 md:grid-cols-[auto_1fr_auto]"><span className="grid h-12 w-12 place-items-center rounded-xl bg-[#f4bc79] text-[#2b1c0b]"><Sparkles className="h-5 w-5"/></span><div><p className="text-sm font-semibold text-[#f4bc79]">Beyond the daily program</p><h2 className="mt-1 text-xl font-semibold text-white">Bonus modules for the conversations that carry more weight.</h2></div><Button onClick={() => setCategory("Leadership")} variant="outline" className="border-[#f4bc79]/40 bg-transparent text-[#ffe7c5] hover:bg-[#f4bc79]/10 hover:text-white">Explore modules <BookOpen className="ml-2 h-4 w-4"/></Button></div></section></div>;
}
