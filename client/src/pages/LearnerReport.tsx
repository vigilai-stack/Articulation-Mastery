import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, Loader2, UserRound } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useLocation } from "wouter";

export default function LearnerReport({ learnerId }: { learnerId: number }) {
  const [, navigate] = useLocation();
  const report = trpc.program.learnerReport.useQuery({ learnerId });
  if (report.isLoading) return <div className="grid min-h-[60vh] place-items-center"><Loader2 className="h-7 w-7 animate-spin text-[#a5f4c9]"/></div>;
  if (report.error || !report.data) return <div className="empty-state"><p>{report.error?.message || "Learner report unavailable."}</p><Button variant="outline" onClick={() => navigate("/reports")}>Return to team insights</Button></div>;
  const data = report.data;
  return <div className="workspace-content"><button onClick={() => navigate("/reports")} className="inline-flex items-center text-sm text-[#a5beb6] hover:text-white"><ArrowLeft className="mr-2 h-4 w-4"/>Team insights</button><section className="mt-7 flex flex-wrap items-end justify-between gap-5"><div><p className="eyebrow">Learner drill-down</p><h1 className="mt-3 font-display text-4xl tracking-[-.03em] text-white sm:text-5xl">{data.learner.name || data.learner.email}</h1><p className="mt-3 text-[#a5beb6]">{data.learner.email}</p></div><div className="rounded-xl border border-white/10 bg-white/[.04] px-5 py-3"><p className="text-xs text-[#9bb4a9]">PROGRAM COMPLETE</p><p className="mt-1 text-2xl font-semibold text-[#a5f4c9]">{data.completionRate}%</p></div></section><section className="mt-8 grid gap-5 xl:grid-cols-[1.1fr_.9fr]"><article className="section-card"><p className="text-sm text-[#9bb4a9]">Measured communication signal</p>{data.weeklyTrend.length ? <div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data.weeklyTrend}><defs><linearGradient id="learnerTrend" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#a5f4c9" stopOpacity={.32}/><stop offset="100%" stopColor="#a5f4c9" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="#ffffff13"/><XAxis dataKey="week" axisLine={false} tickLine={false} tick={{fill:"#9bb4a9",fontSize:11}}/><YAxis domain={[0,100]} axisLine={false} tickLine={false} tick={{fill:"#78948a",fontSize:11}}/><Tooltip contentStyle={{background:"#0d211b",border:"1px solid #ffffff20",borderRadius:12}}/><Area type="monotone" dataKey="score" stroke="#a5f4c9" strokeWidth={2.5} fill="url(#learnerTrend)"/></AreaChart></ResponsiveContainer></div> : <p className="mt-5 text-sm leading-6 text-[#9bb4a9]">No scored practice has been submitted yet.</p>}</article><article className="section-card"><div className="flex items-center gap-2"><UserRound className="h-4 w-4 text-[#f4bc79]"/><p className="text-sm text-[#9bb4a9]">Current capability view</p></div><div className="mt-6 grid grid-cols-2 gap-3">{Object.entries(data.scores).map(([name,score]) => <div key={name} className="rounded-xl bg-white/[.045] p-4"><p className="capitalize text-sm text-[#9bb4a9]">{name}</p><p className="mt-1 text-2xl font-semibold text-[#a5f4c9]">{score || "—"}</p></div>)}</div></article></section><section className="section-card mt-5"><p className="text-sm text-[#9bb4a9]">Practice record</p><div className="mt-5 divide-y divide-white/10">{data.sessions.map(session => <div key={session.id} className="flex flex-wrap items-center justify-between gap-3 py-4"><div><p className="font-medium text-white">Day {String(session.lessonDay).padStart(2,"0")} practice</p><p className="mt-1 text-sm text-[#9bb4a9]">{session.completedAt ? new Date(session.completedAt).toLocaleDateString(undefined,{month:"short",day:"numeric",year:"numeric"}) : "Submitted practice"}</p></div><div className="flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-[#a5f4c9]/10 px-2.5 py-1 text-[#a5f4c9]">Clarity {session.clarityScore}</span><span className="rounded-full bg-white/[.06] px-2.5 py-1 text-[#b2c6be]">Confidence {session.confidenceScore}</span></div></div>)}{!data.sessions.length&&<p className="py-7 text-sm text-[#78948a]">This learner has not submitted a completed practice yet.</p>}</div></section></div>;
}
